<div class="flex flex-col items-center py-24 px-8">

  <%# 아이콘 — 미니멀 북마크 %>
  <div class="mb-8 flex items-center justify-center w-16 h-16 rounded-xl"
       style="background:var(--color-accent-muted);">
    <svg class="w-7 h-7" fill="none" stroke="var(--color-accent)" viewBox="0 0 24 24" stroke-width="1.8">
      <path stroke-linecap="round" stroke-linejoin="round" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"/>
    </svg>
  </div>

  <%# 제목 %>
  <p style="
    font-size:var(--text-xl);
    font-weight:700;
    color:var(--color-text);
    letter-spacing:-0.3px;
    margin-bottom:8px;
    text-align:center;
  ">
    아직 북마크가 없습니다
  </p>

  <%# 설명 %>
  <p style="
    font-size:var(--text-sm);
    color:var(--color-text-tertiary);
    line-height:1.6;
    text-align:center;
    max-width:260px;
    font-weight:400;
  ">
    URL을 추가하면 AI가 자동으로 분석하여 요약과 태그를 생성합니다
  </p>

</div>
